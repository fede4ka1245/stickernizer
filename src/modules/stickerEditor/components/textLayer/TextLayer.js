import React, {useRef} from 'react';
import Player from "../../shared/editor/Player";

const TextLayer = () => {
  const player = useRef(null);
  const targetLayer = useRef(new TextLayer());

  return (
    <>

    </>
  );
};

export default TextLayer;